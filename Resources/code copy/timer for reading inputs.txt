
int main() {
    int  counter= 1;
    clock_t start = clock();
    while (1) {
        if((clock() - start ) >5000 ){
            break;
        }
        if(_kbhit()){
            char userInput = _getch();  // Read the pressed key
            printf("User pressed: %c\n", userInput);
            break;
        }
        printf("hello nega %d \n",(clock() - start ));
    }


    return 0;
}
